/* The <Main /> Component is the top component in the hierarchical structure, and it has two child components in this case;
  <Header /> and <Body />. This will be the Header, hence the file name.
*/

var Header = React.createCLass({
    render() {
        return ( <div>
            <h1> Hello World, Testing React! </h1>
      </div>
        )
    }
});